Create a complete, standalone HTML file for the "Add New Property" form page using HTML + Tailwind CSS 4+ (CDN).

Requirements:
- Top navigation same as Assistant Dashboard.
- Page title: "Add New Property".
- Multi-section form in a centered max-w-4xl card:
  1. Basic Info: Title (text), Property Type (select: Apartment, House, Villa, Land), Price (number), Location (text).
  2. Description: large textarea.
  3. Features: checkbox grid (e.g., Parking, Garden, Pool, Balcony, Furnished).
  4. Images: file input (multiple) with preview placeholder grid (show 4 empty image placeholders).
  5. Submit button: "Submit for Review" (primary, large).
- All fields required with clear labels and placeholders.
- Responsive: stacked on mobile, 2-column where possible on desktop.
- Accessible form with proper labels and error placeholders.
- Professional, spacious Tailwind design.
- Include Tailwind CDN.

Output only the full HTML code in a ```html block.